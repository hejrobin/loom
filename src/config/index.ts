import $pkg from '../../package.json';
import $prj from '../../project.json';

export const pkg = {
	name: $pkg.name,
	version: $pkg.version,
};

export const project = {
	name: $prj.name,
	version: $prj.version,
};
